#include"3-calc.h"
/**
 *op_add -  add two numbers
 *@a: the first number
 *@b: the second number
 *Return: the addition of the two numbers
 */
int op_add(int a, int b)
{
	return (a + b);
}
/**
 *op_sub - return the difference between two numbers
 *@a: the first number
 *@b: the second number
 *Return: th edifference between the two numbers
 */
int op_sub(int a, int b)
{
	return (a - b);
}
/**
 *op_mul - return the multiplication of two numbers
 *@a: the first number
 *@b: the second number
 *Return: the multiplication of two numbers
 *
 */
int op_mul(int a, int b)
{
	return (a * b);
}
/**
 *op_div - divise two numbers
 *@a: the first number
 *@b: the second number
 *Return: division between two numbers
 */
int op_div(int a, int b)
{
	return (a / b);
}
/**
 *op_mod - calculate the module of two numbers
 *@a: the first number
 *@b: the second number
 *Return: moduele of two numbers
 *
 */
int op_mod(int a, int b)
{
	return (a % b);
}

