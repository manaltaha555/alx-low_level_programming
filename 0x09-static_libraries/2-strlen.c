#include <string.h>
#include"main.h"
/**
 *_strlen - returns the length of a string
 *@s: is a pointer to a string
 *Return: the length of a string
 *
 */
int _strlen(char *s)
{
	return (strlen(s));

}
